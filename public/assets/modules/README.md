This directory stores uploaded/generated module assets and is ignored by git.
If you need the empty directory tracked, keep the .gitkeep file.
Ensure your deployment or storage persists this directory (or use external storage like S3).
